```
Proceso a:
    while(true):
        res,telemetria=computo imporantisimo
        bsend(b,&res) //compartir dato que no puede esperar
        if(!send(telemetria)) //si se pierde un dato no es relevante
            continue

Proceso b:
    res,t;
    while(true):
        breceive(a,&res)
        compartir_resultados_urgentes(res)
        receive(a,&t)
        if(t) save_telemetria(t)
```
