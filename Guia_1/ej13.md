a.

```c
int result;
void proceso_izquierda() {
    result = 0;
    while (true) {
        while(! send(pid_derecha, &result)); // Mientras la cola esta llena seguir itentando mandar
        result = cómputo_muy_difícil_1();
    }
}
void proceso_derecha() {
    int left_result;
    while(true) {
        result = cómputo_muy_difícil_2();
        while(! receive(pid_izquierda, &left_result)); // Espera resultado
        printf("%s %s", left_result, result);
    }
}
```

Notar ambos son "busy waiting"

b. 1 tambien.
Esto garantizaria que el primer while de izq se hace, y se ponen a,bos a ejecutar casi a la vez el computo dificil, estando practicamente sincronizados y evitando el problema origianl del 12.
