1)
```c
int ticket = 0;
int turno = 0;

void proc() {
    cosas_no_criticas();
    int miTurno = obtenerTurno();
    while (miTurno != turno);
    funcion_critica();  // sección crítica
    terminarTurno();
    cosas_no_criticas();
}

obtenerTurno() {
    int temp = ticket;
    ticket++;
    return temp;
}

terminarTurno() {
    turno++;
}
```

a) Si dos procesos ejecutan este código de manera concurrente, determinar si al utilizar las funciones obtenerTurno y terminarTurno se delimita correctamente una sección crítica. En caso contrario, mostrar una traza que ejemplifique el problema. Justificar.

Supongamos que ejecuta de la siguiente manera
```c
// # PROC 1
cosas_no_criticas();
// Obtener turno
int temp = ticket; //(0)
// # PROC 2
cosas_no_criticas();
// Obtener turno
int temp = ticket; //(0)
```
En este momento temp vale lo mismo para ambos (0)

```c
// # PROC1
ticket++;
return temp;
// end obtener turno -> miTurno = 0 y ticket = 1

// # PROC2
ticket++;
return temp;
// end obtener turno -> miTurno = 0 y ticket = 1
```
Claramente esto no esta bien, tocket deberia ser igual a 2, y los turnos 0 y 1

```c
// # PROC1
while (miTurno != turno); //turno = 0 , miTurno = 0. OK
funcion_critica();  // sección crítica
//terminarTurno
turno++; // (1)
cosas_no_criticas();

// # PROC2
while (miTurno != turno); //turno = 1 , miTurno = 0. FALSE
```
Nunca, proc2 va a poder pasar este while.


b) Si hay un problema con la exclusión mutua, ¿cómo se podría arreglar? Justificar cómo y con qué mecanismos de software/hardware.

Para solucionarlo, necesitariamos semaforos, para esto necesitamos el int atomico por parte del harware, que tiene sus debidas instrucciones, getAndInc, set, etc
Por parte del SO, necesitariamos que soporte soporte esta implementacion de semaforo, pueda poner procesos a dormir y despertarlos cuando el semaforo tenga > 0 su variable interna, y los despierte del wait.

Bastaria un semaforo tipo mutex (1 solo proceso), y rodear con wait y signal obtener el turno y terminarlo. O 2 mutex para mas eficiencia, 1 al obtener y otro a terminar, ya que no modifican el mismo recurso.

Otra forma posible seria 

.
Okey el problema no era lo del lock del while, sino que enraban a la funcion critica al mismo tiempo,, okey, entonces supongamos que lo deja pasar del while el scheduler.
seria
Proc1
while()
Proc2
while
Ahi ambos pasan poruqe turno sigue valiendo 0
Ahora ambos pueden ejecutar funcion critica al mismo tiempo. El mutex en la funcion
obtener turno sigue siendo la solucion. Y en el turno++ no hace realmente falta porque el while si hace de barrera si el lturno es correctamente asignado



2)
Ejercicio 2.
Sea un sistema donde un proceso parent crea un child y el child crea un grandchild. El grandchild debe imprimir "Soy el mejor nieto", luego dormir por 60 segundos, una vez pasado el minuto avisar al child que se despertó usando pipes y por último colgarse con un while(TRUE);. Al llegar al punto de cuelgue y recibir el mensaje por parte del grandchild, el child debe matarlo y hacer un exit. Por último el parent espera a que el child termine e imprime "Mi hijo querido se ha ido" y termina.

a) Escribir un programa (en pseudocódigo) que se comporte como el sistema descrito, utilizando las syscalls necesarias. Dado que se trata de pseudocódigo, no es necesario ser estricto con los parámetros y los valores de retorno de los syscalls, aunque sí respetar su espíritu.
b) Describir en qué estado queda cada uno de los procesos involucrados luego de la ejecución de cada syscall.

El ejercicio no aclaraba de cerrar pipes...andaria igual sin cerrarlos  en este caso
```c
padre:
    pid = fork()
    if pid == 0
        son()
    waitforpid(pid)
    printf("termino mi hijo");
    exit()
    

son()
    pipes[2];
    pipe(pipes);
    pid = fork()
    if pid == 0
        grandson(pipes)
    //close(pipe[write]);
    read(pipe[read],int value,sizeofwalue);
    //close(pipe[read]);
    kill(pid, sigkill);
    exit()

grandchild(pipes)
    //close(pipe[read])9
    printf('SOY EL MEJOR');
    sleep(60);
    write(pipe[write],1,sizeof(1));
    //close(pipe[write])
    while(1)
```c

b)
Las syscals son 
fork 
waitfor
pipe
fork
close
read
write
sleep
kill
printf
exit, lo sume al final

Todos son wrappers.
ece def
en fork1. padre running y child ready
pipe, son running padre en ready.
en print so el mejor, grand child running, child y padre en ready
sleep grandchild pasa a a bloqued, y el resto estaba en ready.
read, pasa a bloqueado el son, el grandchild estaria en el sleep bloqeuado y padre ready
waitforpid, el padre pasa a bloquado, esperando a que termine child. el resto sigue bloquado
Notar que otra traza, podria haber primero hecho el read o el waitfor, eso cambiaria quien se bloquea...nose que esperaban aca poner exactamente
Write, esta en running el child, el resto en bloquead
kill, se levanto el child al terminar el read, el padre sigue bloqued y el grandchild running en busy waiting. Al terminar el kill el grandson paso a ser zombie.

Termina el waitforpid, ambos child y grandchild estan terminados, ya no zombie porque el child fue limpiado correctamente


3.
a)
Se usa el (0 stdin), el (1, stdout), definidos por el sistemam y 3,4,5,6 fd creados por mediante pipe, son solicitdos por el programa.
b) Es interactivo, pide entrada por la terminal write(0). Ademas muestra respuestas al finalizar (llegamos), no se si cuenta eso. EL UNICO WRITE A CONSOLA es "llegamos" no?

```c
p1[2];
p2[2];
pipe(p1);
pipe(p2);

pid_t pid = fork()
if(pid ==0){ //(4673)
    close(p1[1]);
    close(p2[0]);
    char buffer[1024];
    read(p1[0],&buffer,sizeof(buffer));
    char response1[16] = "MENSAJE ERRONEO\n"; 
    write(p2[1],&response1,sizeof(response1));
    
}
close(p1[0]);
close(p2[1]);
char buffer[1024];
read(stdin,&buffer,sizeof(buffer)); // 0 es std in, es un input de python esto
write(p1[1], &buffer, sizeof(buffer)); // anda
clearbuffer(&buffer,1024); // sin syscall, nose si hay otra forma mejor? crear otro buffer? o poner que lees desde arriba...calculo que es lo mejor
read(p2[0],&buffer,sizeof(buffer));
write(p1[1], &buffer, sizeof(buffer)); //vamos
clearbuffer(&buffer,1024);

write(p1[1], &buffer, sizeof(buffer)); //IR!
clearbuffer(&buffer,1024);


clearbuffer(&buffer,len){
    for(int i =0;i<len;i++){
        buffer[i]= 0;
    }
}
```
